# Vectrax — Sesión 2026-06-08: Auditoría y Corrección del Pipeline

## Producción
- **Servidor:** 140.82.28.181 (Vultr)
- **Commit:** `08eca4d` (main)
- **Container:** vectrax-core (healthy)
- **Puerto:** 8900

## Cambios Desplegados

### 1. `/lead <nombre>` resuelve intención (PR #13)
**Archivo:** `vectrax/telegram_gateway.py` → `_handle_lead()`

Antes: `/lead joy` mostraba lista de comandos.
Ahora: busca lead "Joy" → si existe muestra detalle, si no ofrece crear.
Ayuda solo aparece con `/lead help`.

### 2. Respuesta de memoria sintetizada
**Archivo:** `vectrax/core_memory.py` → `get_living_response()`

Antes: "Que sabes de mi?" volcaba 50+ fragmentos crudos (2000+ chars).
Ahora: max 6 fragmentos, priorizados por peso, deduplicados. ~200 chars.

### 3. Extractores de core_memory endurecidos
**Archivo:** `vectrax/core_memory.py` → `_EXTRACTORS` + `_EXTRACTION_BLACKLIST`

- Regex `work`: ya no matchea "soy" + cualquier cosa. Solo profesiones conocidas.
- Regex `goal`: eliminado "quiero/necesito". Solo "mi meta es", "estoy construyendo".
- Blacklist de 30+ patrones garbage de conversaciones reales.
- Ubicación requiere ciudad con mayúscula.

### 4. API Gate integrado en pipeline
**Archivos:** `vectrax/nucleus_resolver.py`, `vectrax/self_context.py`

`check_gate("openai")` + `record_429()` / `record_success()` añadidos a los
call sites del pipeline principal que no lo tenían.

### 5. Rutas duplicadas eliminadas del gateway
**Archivo:** `vectrax/telegram_gateway.py`

- **Market detection** removida del gateway → solo SmartRouter via queue.
- **Fast-path** removido del gateway → solo ExternalGateway decide (respeta creator bypass).

### 6. Gravity lock para "continuidad"
**Archivo:** `core/word_gravity.py` → `_SEED_WORDS`

Palabras añadidas al seed global:
- `continuidad` (0.86) — prevenía búsqueda web de BBC Radio / electricidad
- `coherencia` (0.78), `conciencia` (0.76), `aprendizaje` (0.74), `router` (0.72)

### 7. Meta_loop pasa por Telegram Guard
**Archivo:** `core/meta_loop.py` → `_send_telegram()`

Antes: HTTP directo a Telegram API sin filtro → `gravity/universe_growth` llegaba al chat.
Ahora: pasa por `should_send_to_user()` antes de enviar.

`universe_growth`, `convergence_detected`, `daily_reflection` removidos de `_ALERT_OBS_TYPES`.

### 8. Cadena de fallback de 3 niveles
**Archivos:** `core/operator/external_gateway.py`, `core/transport/pipeline_worker.py`

```
Intelligence Bridge (OpenAI/Gemini) → OpenAI directo → Ollama local
→ Memory synthesis (sin LLM) → "Capacidad limitada" (graceful degradation)
```

El usuario NUNCA recibe silencio.

### 9. Limpieza de DB de producción
125 entradas garbage eliminadas de `core_memory` (325 → 200).
- 15 work entries ("Trabaja como yo quien diseñó imagino y")
- 76+ goal entries ("Quiere que me hagas un análisis")
- 2 identity entries ("vivo en evolución continua")
- 1 relationship entry ("no es su socia")

## Tests
- `tests/test_system_e2e_final.py` — 100 tests (12 subsistemas)
- `tests/test_lead_intent_resolution.py` — 10 tests
- `tests/test_living_response.py` — 18 tests
- `tests/test_word_gravity.py` — 37 tests (existente)
- **Total nuevos:** 128 tests, todos pasan.
- **Suite completa:** 2292 passed, 0 regresiones.

## Stress Test (producción)
```
Gravity lock:    5/5 PASS
Living response: PASS (206 chars, zero garbage)
Telegram guard:  7/7 PASS
Concurrent:      5/5 PASS (gates cerrados, 5 threads)
Overall:         ALL PASS
```

## Arquitectura del Pipeline (estado actual)

```
Telegram → Gateway (_handle)
  ├── Comandos directos (/lead, /help, /vx, /team, /tasks)
  ├── Weather, Image gen, Scheduling → respuesta directa
  └── ENQUEUE → SQLite queue
                    │
          Pipeline Worker (_process_one)
            ├── Scale Governor preflight
            ├── Convergence cycle (7 fases)
            ├── ExternalGateway.receive_message()
            │     ├── Identity Anchor
            │     ├── Intake Filter
            │     ├── Word Gravity Activation ← gravity_lock
            │     ├── Self-aware context
            │     ├── Nucleus Resolver
            │     ├── Resolve from Memory
            │     ├── SmartRouter pipeline
            │     │     ├── RESOLVE_MARKET
            │     │     ├── RESOLVE_IDENTITY
            │     │     ├── RESOLVE_ONLINE (blocked by gravity_lock)
            │     │     ├── RESOLVE_LOCAL
            │     │     └── LLM (Intelligence Bridge → OpenAI → Ollama → Memory synthesis)
            │     ├── Response Consolidator
            │     ├── Identity Layer
            │     └── Identity Guard
            ├── Language Enforcement
            ├── Anti-repetition Filter
            ├── Graceful Degradation (never silence)
            └── _tg_send() → Telegram Guard → Telegram API → Voice dispatch
```

## Pendiente (no tocar todavía)
- **F8:** Consolidar 24 pre-filtros del gateway en dispatcher
- **F9:** Bus legacy (observacional, no resolutivo)
- **F10:** Convergence cycle optimization
- **API Gate:** 8 call sites menores aún sin protección (face_memory, preference_tracker, etc.)
- **Ollama:** No instalado en el servidor Vultr — instalar para activar fallback local

## Co-Authored-By
Oz <oz-agent@warp.dev>
