# Vectrax Production Dependencies
# ================================

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-multipart

# HTTP clients
httpx[http2]>=0.27.0
requests>=2.32.0

# Environment
python-dotenv>=1.0.0

# ML / Embeddings
numpy>=2.0.0
sentence-transformers>=5.1.0

# Filesystem monitoring
watchdog>=6.0.0

# Payments
stripe>=11.0.0

# Brokers — Alpaca connector (connectors/alpaca/alpaca_client.py).
# Modern official SDK (module `alpaca`), NOT the legacy `alpaca-trade-api`.
# Required so BROKER_PROVIDER=alpaca can import the client at runtime.
alpaca-py>=0.33.0
